header.header
    .header__column
        a(href=routes.home)
            i.fab.fa-youtube
    .header__column
        form(action=routes.search, method="get") 
            input(type="text", placeholder="Search by term. . .", name="term")
            //- method="get"이 url에 정보 추가 (controller로 query에 접근하기 위해)
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Log In
        